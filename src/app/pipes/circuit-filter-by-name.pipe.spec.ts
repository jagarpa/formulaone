import { CircuitFilterByNamePipe } from './circuit-filter-by-name.pipe';

describe('CircuitFilterByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CircuitFilterByNamePipe();
    expect(pipe).toBeTruthy();
  });
});

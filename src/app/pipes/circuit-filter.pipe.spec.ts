import { CircuitFilterPipe } from './circuit-filter.pipe';

describe('CircuitFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CircuitFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
